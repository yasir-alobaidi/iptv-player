diff -ru a/media_kit_video/linux/video_output.cc b/media_kit_video/linux/video_output.cc
--- a/media_kit_video/linux/video_output.cc	2026-09-15 10:47:21.471975684 -0400
+++ b/media_kit_video/linux/video_output.cc	2026-09-15 10:47:21.509975404 -0400
@@ -146,7 +146,29 @@
     EGLSurface flutter_draw_surface = eglGetCurrentSurface(EGL_DRAW);
     EGLSurface flutter_read_surface = eglGetCurrentSurface(EGL_READ);
     
-    if (flutter_display != EGL_NO_DISPLAY && flutter_context != EGL_NO_CONTEXT) {
+    // iptv-player patch (media_kit #1404): since Flutter 3.38 no EGL context
+    // is current on the platform thread. Get the same EGLDisplay Flutter uses
+    // from GDK's native display instead.
+    if (flutter_display == EGL_NO_DISPLAY) {
+      GdkDisplay* gdk_display = gdk_display_get_default();
+      if (GDK_IS_WAYLAND_DISPLAY(gdk_display)) {
+        flutter_display = eglGetPlatformDisplayEXT(
+            EGL_PLATFORM_WAYLAND_EXT,
+            gdk_wayland_display_get_wl_display(gdk_display), NULL);
+      } else if (GDK_IS_X11_DISPLAY(gdk_display)) {
+        flutter_display = eglGetPlatformDisplayEXT(
+            EGL_PLATFORM_X11_EXT, gdk_x11_display_get_xdisplay(gdk_display),
+            NULL);
+      }
+      if (flutter_display != EGL_NO_DISPLAY &&
+          !eglInitialize(flutter_display, NULL, NULL)) {
+        flutter_display = EGL_NO_DISPLAY;
+      }
+      g_print("media_kit: VideoOutput: No current EGL context; using GDK's EGLDisplay %p.\n",
+              flutter_display);
+    }
+
+    if (flutter_display != EGL_NO_DISPLAY) {
       self->egl_display = flutter_display;
       
       // Bind OpenGL ES API (Flutter uses OpenGL ES on Linux)
@@ -156,7 +178,8 @@
       EGLConfig config = NULL;
       EGLint config_id = 0;
       
-      if (eglQueryContext(self->egl_display, flutter_context, EGL_CONFIG_ID, &config_id)) {
+      if (flutter_context != EGL_NO_CONTEXT &&
+          eglQueryContext(self->egl_display, flutter_context, EGL_CONFIG_ID, &config_id)) {
         g_print("media_kit: VideoOutput: Flutter's EGL config ID: %d\n", config_id);
         
         // Get Flutter's exact config
@@ -169,11 +192,23 @@
           g_printerr("media_kit: VideoOutput: Failed to get Flutter's EGL config by ID.\n");
           config = NULL;
         }
-      } else {
+      } else if (flutter_context != EGL_NO_CONTEXT) {
         g_printerr("media_kit: VideoOutput: Failed to query Flutter's EGL config ID.\n");
       }
       
-      if (config != NULL) {        
+      // iptv-player patch: no Flutter context to copy the config from.
+      if (config == NULL) {
+        EGLint attribs[] = {EGL_RENDERABLE_TYPE, EGL_OPENGL_ES2_BIT,
+                            EGL_RED_SIZE, 8, EGL_GREEN_SIZE, 8, EGL_BLUE_SIZE, 8,
+                            EGL_ALPHA_SIZE, 8, EGL_NONE};
+        EGLint num_configs = 0;
+        if (!eglChooseConfig(self->egl_display, attribs, &config, 1, &num_configs) ||
+            num_configs == 0) {
+          config = NULL;
+        }
+      }
+
+      if (config != NULL) {
         // Create an isolated EGL context (NOT shared with Flutter)
         // This prevents OpenGL state pollution and resource contention
         EGLint context_attribs[] = {
