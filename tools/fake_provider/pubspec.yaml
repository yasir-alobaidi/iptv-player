name: fake_provider
description: Fake IPTV provider (Xtream API + looped sample streams) for tests and manual runs. See docs/06-quality.md.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  http: ^1.5.0
  test: ^1.26.0
  very_good_analysis: ^11.0.0
